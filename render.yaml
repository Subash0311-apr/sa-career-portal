services:
  - type: web
    name: sa-career-page
    runtime: static
    buildCommand: npm ci && npm run build:render
    staticPublishPath: dist/sa-career-page/browser
    envVars:
      - key: NODE_VERSION
        value: 20
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
